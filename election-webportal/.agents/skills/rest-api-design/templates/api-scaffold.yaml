# API Endpoint Scaffold
# Customize for your API framework

openapi: "3.0.3"
info:
  title: "API Service"
  version: "1.0.0"

paths:
  /api/v1/resource:
    get:
      summary: "List resources"
      # Define parameters and responses here
      responses:
        "200":
          description: "Success"
    post:
      summary: "Create resource"
      # Define request body and responses here
      responses:
        "201":
          description: "Created"
